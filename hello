# os library in use
import os

# clear line of code after you run the code blow
os.system('clear')

# ariables
x=1

# strings
my_name = "eddie the legend says hello world"

# lists and touples
the__legends__goodwill =[0,1,2,3,4,5]

the__number_of_enemies_defeated = {
	"imposible": "zero enemies",
	"very possible": "power level over 10000"
}
answer = "very possible"

age = 26

why = answer


#boolean, uses if staements, else statements etc


#hello world code
print(my_name.split(" ")+the__legends__goodwill)
print(the__number_of_enemies_defeated["very possible"])



# .ssh test, using git hub to save your progress online 
# got to "johnelder.com/code/" follow codes blow to push to git  hub
#git add .
#git commi -am "type in description here"
#git push


print(len(my_name[0:5].split(" ")))
print(len(my_name[7:9].split(" ")))
print(len(my_name[11:16].split(" ")))
print(len(my_name[18:21].split(" ")))
print(len(my_name[23:27].split(" ")))
print(len(my_name[29:33].split(" ")))